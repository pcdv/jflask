subprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'

    version = "0.2-SNAPSHOT"

    repositories {
         mavenCentral()
    }

    dependencies {
        testCompile "junit:junit:4.11"
    }

    eclipse {
      classpath {
        defaultOutputDir = file('classes')
      }
    }
}
